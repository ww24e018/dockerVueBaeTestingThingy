# dockerVueIntegrationPlayground
for testing/learning

# lePlan

- do all node/npm stuff that actually runs in a container
- make code that does the vue-stuff there
- but first: make the "some node code + scripts that configure it in a container"

# what was

environment: ide-local node-installation   


init vue scaffold mit   
`npm create vue@latest`   

Features choose by altin-guidance: typescript, router, pinia

`npm i` == `npm install`  

`npm run dev` -> local dev server mit sonstigen anleitungen.

## tipps

for looking at the build output   
`npx vite preview`  
`npx vite build`   

